import {StyleSheet, View} from 'react-native';

const ListCellSeperatorView = () => {
  return <View style={styles.container} />;
};

const styles = StyleSheet.create({
  container: {
    height: 0.5,
    backgroundColor: 'black',
  },
});

export default ListCellSeperatorView;
